const btn = document.querySelector('.colorselector');

    btn.addEventListener('click',() =>{
        let [tab] chrome,tabs,query ({ active: true, currentWindow: true });
        console.log(tab)
});